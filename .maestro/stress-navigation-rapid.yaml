# Stress Test: Rapid Navigation
# Tests app stability under rapid screen transitions

appId: com.veloq.app.dev
tags:
  - tier3
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

# Rapid tab switching (5 full cycles)
- repeat:
    times: 5
    commands:
      - openLink: "veloq://fitness"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://training"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://map"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"

# Rapid secondary screen access
- repeat:
    times: 3
    commands:
      - openLink: "veloq://settings"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://about"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://routes"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"

# Rapid activity detail access
- repeat:
    times: 5
    commands:
      - openLink: "veloq://activity/demo-test-0"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://activity/demo-test-1"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://activity/demo-2"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"
      - openLink: "veloq://"
      - runFlow:
          when:
            visible: "Open"
          commands:
            - tapOn: "Open"

# Verify home is still responsive (longer timeout after rapid navigation)
- extendedWaitUntil:
    visible:
      id: "home-screen"
    timeout: 60000

- extendedWaitUntil:
    visible:
      id: "home-activity-list"
    timeout: 15000

- takeScreenshot: "stress-navigation-rapid-complete"
