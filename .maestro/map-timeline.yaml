# Map Timeline Slider Tests
# Tests the timeline slider on the map screen

appId: com.veloq.app
tags:
  - tier2
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

- runFlow:
    file: helpers/navigate-to-map.yaml

# Verify timeline slider exists
- extendedWaitUntil:
    visible:
      id: "timeline-slider"
    timeout: 10000

- takeScreenshot: "map-timeline-complete"
