# Activity Charts Interaction Tests
# Tests chart type selector on activity detail screen

appId: com.veloq.app.dev
tags:
  - tier2
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

# Wait for demo data to load before deep linking
- extendedWaitUntil:
    visible:
      id: "home-activity-list"
    timeout: 15000

# Navigate to activity detail
- openLink: "veloq://activity/demo-test-0"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"

- extendedWaitUntil:
    visible:
      id: "activity-detail-content"
    timeout: 15000

- extendedWaitUntil:
    visible:
      id: "chart-type-selector"
    timeout: 10000

# Toggle different chart types
- tapOn:
    id: "chart-type-heartrate"
    optional: true

- takeScreenshot: "activity-charts-heartrate"

- tapOn:
    id: "chart-type-power"
    optional: true

- takeScreenshot: "activity-charts-power"

- tapOn:
    id: "chart-type-elevation"
    optional: true

- takeScreenshot: "activity-charts-elevation"

- tapOn:
    id: "chart-type-speed"
    optional: true

- takeScreenshot: "activity-charts-speed"

- takeScreenshot: "activity-charts-complete"
