# Fitness Metrics Tests
# Tests the fitness chart CTL/ATL values display

appId: com.veloq.app.dev
tags:
  - tier2
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

- runFlow:
    file: helpers/navigate-to-fitness.yaml

# Verify CTL (fitness) value is displayed
- extendedWaitUntil:
    visible:
      id: "fitness-ctl-value"
    timeout: 10000

# Verify CTL value is not NaN
- assertNotVisible:
    text: "NaN"

# Verify ATL (fatigue) value is displayed
- assertVisible:
    id: "fitness-atl-value"

# Verify ATL value is not NaN
- assertNotVisible:
    text: "NaN"

- takeScreenshot: "fitness-metrics-ctl-loaded"

# Scroll down to form zone chart area
- swipe:
    start: 50%, 75%
    end: 50%, 25%
    duration: 500

# Verify form zone chart rendered
- assertVisible:
    id: "fitness-form-zone-chart"
    optional: true

# Verify activity dots chart rendered
- assertVisible:
    id: "fitness-activity-dots"
    optional: true

- takeScreenshot: "fitness-form-zone-and-dots"

- takeScreenshot: "fitness-metrics-complete"
