# Settings Persistence Test
# Verifies theme setting persists across navigation

appId: com.veloq.app.dev
tags:
  - tier2
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

- runFlow:
    file: helpers/navigate-to-settings.yaml

# Theme buttons live inside the Display section, several screens below the
# fold. Scroll directly to the button so it's in the queryable view tree.
- scrollUntilVisible:
    element:
      id: "theme-button-dark"
    direction: DOWN
    timeout: 15000
    speed: 40
    visibilityPercentage: 20

# Switch to dark theme
- tapOn:
    id: "theme-button-dark"

- takeScreenshot: "settings-persistence-dark-set"

# Navigate away to fitness
- openLink: "veloq://fitness"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"
- extendedWaitUntil:
    visible:
      id: "fitness-screen"
    timeout: 10000

- takeScreenshot: "settings-persistence-navigated-away"

# Navigate back to settings
- openLink: "veloq://settings"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"
- extendedWaitUntil:
    visible:
      id: "settings-screen"
    timeout: 10000

# Re-scroll Display section into view after the round-trip.
- scrollUntilVisible:
    element:
      id: "theme-button-dark"
    direction: DOWN
    timeout: 15000
    speed: 40
    visibilityPercentage: 20

# Verify dark theme button is still visible (settings still accessible)
- assertVisible:
    id: "theme-button-dark"

- takeScreenshot: "settings-persistence-returned"

# Reset to system theme
- tapOn:
    id: "theme-button-system"

- takeScreenshot: "settings-persistence-complete"
