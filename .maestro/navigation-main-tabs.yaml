# Main Tab Navigation Tests
# Tests navigation between the main bottom tabs

appId: com.veloq.app
tags:
  - tier2
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

- runFlow:
    file: helpers/navigate-to-fitness.yaml

- runFlow:
    file: helpers/navigate-to-training.yaml

- runFlow:
    file: helpers/navigate-to-map.yaml

# Navigate back to Home
- openLink: "veloq://"
- extendedWaitUntil:
    visible:
      id: "home-screen"
    timeout: 10000
