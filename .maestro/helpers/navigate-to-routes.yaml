# Navigate to routes tab via deep link
appId: com.veloq.app.dev
---
- openLink: "veloq://routes?tab=routes"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"
- extendedWaitUntil:
    visible:
      id: "routes-screen"
    timeout: 10000
