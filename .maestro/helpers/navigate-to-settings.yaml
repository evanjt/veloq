# Navigate to settings screen via deep link
appId: com.veloq.app.dev
---
- openLink: "veloq://settings"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"
- extendedWaitUntil:
    visible:
      id: "settings-screen"
    timeout: 10000
