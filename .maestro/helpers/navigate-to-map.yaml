# Navigate to map tab via deep link
appId: com.veloq.app.dev
---
- openLink: "veloq://map"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"
- extendedWaitUntil:
    visible:
      id: "map-screen"
    timeout: 30000
