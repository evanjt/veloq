# Navigate to training tab via deep link
appId: com.veloq.app.dev
---
- openLink: "veloq://training"
- runFlow:
    when:
      visible: "Open"
    commands:
      - tapOn: "Open"
- extendedWaitUntil:
    visible:
      id: "training-screen"
    timeout: 10000
