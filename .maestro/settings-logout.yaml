# Settings Logout Test
# Tests logout flow returns to login screen

appId: com.veloq.app.dev
tags:
  - tier2
---
- runFlow:
    file: helpers/setup-demo-mode.yaml

- runFlow:
    file: helpers/navigate-to-settings.yaml

# Scroll to find logout button (accept partial visibility)
- scrollUntilVisible:
    element:
      id: "settings-logout-button"
    direction: DOWN
    timeout: 30000
    speed: 40
    visibilityPercentage: 20

# Extra swipe to move it well above the tab bar
- swipe:
    direction: UP
    duration: 500

# Tap logout button (triggers confirmation dialog)
- tapOn:
    id: "settings-logout-button"

# Wait for the disconnect alert title to appear
- extendedWaitUntil:
    visible:
      text: "Disconnect Account"
    timeout: 5000

- takeScreenshot: "settings-logout-dialog"

# Tap the destructive "Disconnect" button
- tapOn:
    text: "^Disconnect$"

# Verify we return to login screen (may take time to tear down)
- extendedWaitUntil:
    visible:
      id: "login-screen"
    timeout: 20000

- takeScreenshot: "settings-logout-complete"

# Re-enter demo mode for subsequent tests
- tapOn:
    id: "login-demo-button"

# Dismiss any post-login dialogs
- runFlow:
    when:
      visible: "Not Now"
    commands:
      - tapOn: "Not Now"

- runFlow:
    when:
      visible: "Done"
    commands:
      - tapOn: "Done"

- runFlow:
    when:
      visible: "Skip"
    commands:
      - tapOn: "Skip"

- extendedWaitUntil:
    visible:
      id: "home-screen"
    timeout: 30000
