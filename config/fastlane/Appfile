# Android
json_key_file(ENV["SUPPLY_JSON_KEY"])
package_name("com.veloq.app")

# iOS (configure when Apple Developer account is ready)
app_identifier("com.veloq.app")
apple_id(ENV["FASTLANE_USER"])
team_id(ENV["FASTLANE_TEAM_ID"])
