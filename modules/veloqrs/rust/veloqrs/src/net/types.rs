//! serde response types and parsed output records for intervals.icu endpoints.
//!
//! Filled in per endpoint slice. Each raw `*Dto` mirrors the JSON the server
//! returns; the parsed record is the shape the app consumes (matching the old
//! TypeScript return types).
