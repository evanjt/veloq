{
  "name": "veloqrs",
  "version": "0.1.0",
  "description": "Native Rust-powered route matching for Expo/React Native",
  "author": "Evan Thomas <evan@evanjt.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/evanjt/veloq"
  },
  "homepage": "https://github.com/evanjt/veloq",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "react-native": "src/index.ts",
  "scripts": {
    "clean": "./scripts/clean.sh",
    "generate:android": "npx uniffi-bindgen-react-native build android --release --and-generate --targets ${VELOQ_ANDROID_TARGET:-arm64-v8a} && npm run fix-includes",
    "generate:android:all": "npx uniffi-bindgen-react-native build android --release --and-generate && npm run fix-includes",
    "generate:ios": "npx uniffi-bindgen-react-native build ios --release --and-generate --targets ${VELOQ_IOS_TARGET:-aarch64-apple-ios-sim} && npm run fix-includes",
    "generate:ios:all": "npx uniffi-bindgen-react-native build ios --release --and-generate && npm run fix-includes",
    "fix-includes": "./scripts/fix-generated.sh",
    "android": "expo run:android",
    "ios": "expo run:ios"
  },
  "peerDependencies": {
    "expo": "*",
    "react": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "expo-modules-core": "*"
  },
  "dependencies": {
    "expo": "~54.0.30",
    "react": "19.1.0",
    "react-native": "0.81.5"
  }
}
