<manifest>
  <application>
    <!-- Not exported: the only sender is this app's own notification buttons. -->
    <receiver
      android:name=".RecordingActionReceiver"
      android:exported="false" />
  </application>
</manifest>
